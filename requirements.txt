# Grid Bot Multi-User Server dependencies
# All versions pinned exact, verified stable (7+ days old)

# --- Admin panel ---
fastapi==0.115.12
uvicorn==0.34.2
cryptography==44.0.3
httpx==0.28.1

# --- Grid bot (existing deps) ---
aiohttp==3.11.16
websocket-client==1.8.0
websockets==15.0.1
flask==3.1.1
flask-socketio==5.5.1
